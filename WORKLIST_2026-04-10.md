# PepeClaw Office Viewer Worklist — 2026-04-10

## Goal
Finish and polish the 3D office viewer so it is ready for browser verification, demo capture, and public sharing.

## What is already true
- Loading bug was fixed on 2026-03-29 (`66f0fd3`): lazy import + R3F hook issue removed.
- Phaser.js rebuild landed on 2026-03-30.
- Current key files:
  - `src/phaser/OfficeScene.ts`
  - `src/phaser/AgentSprite.ts`
  - `src/components/PhaserScene.tsx`
  - `src/App.tsx`
  - `src/components/ActivityFeed.tsx`
  - `src/api/DataProvider.tsx`
- Build passes.
- Tests pass: 93/93.

## Remaining work from prior chat/memory
1. **Browser-verify localhost:5173** and inspect the current Phaser office viewer with real visuals.
2. **Close the remaining visual gap** to the hero/reference look:
   - richer interiors
   - denser room layout
   - more solid/opaque room presence
   - stronger neon/holographic feel
   - bigger / clearer agents
   - better sense of life and motion
3. **Polish the viewer UX**:
   - camera feel
   - activity feed / minimap / room readability
   - screenshot-worthy presentation
4. **Capture demo-ready assets**:
   - screenshots
   - short demo flow
   - README / launch polish
5. **Clean small tech debt found today**:
   - React test `act(...)` warnings in `tests/dataProvider.test.ts`
   - large bundle warning from build output

## Important product direction
- Mythos should be treated as a **model-agnostic runtime advantage layer** for a later phase, not as a base-model replacement.
- First priority right now is still the office viewer finish/polish pass.
